{% extends 'base.html.twig' %}

{% block title %}Veterinarian{% endblock %}

{% block body %}
    <h1>Veterinarian</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ veterinarian.id }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ veterinarian.lastname }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ veterinarian.password }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ veterinarian.mail }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_veterinarian_index') }}">back to list</a>

    <a href="{{ path('app_veterinarian_edit', {'id': veterinarian.id}) }}">edit</a>

    {{ include('veterinarian/_delete_form.html.twig') }}
{% endblock %}
