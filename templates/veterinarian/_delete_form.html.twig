<form method="post" action="{{ path('app_veterinarian_delete', {'id': veterinarian.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ veterinarian.id) }}">
    <button class="btn">Delete</button>
</form>
